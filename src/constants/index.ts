// Hot prompts IDs that should appear at the top
export const HOT_IDS = [1, 2, 17, 18];

// Local storage keys
export const STORAGE_KEYS = {
  FAVORITES: 'favoritePrompts',
  THEME: 'darkMode',
  RECENT_PROMPTS: 'recentPrompts',
} as const;

// Animation durations
export const ANIMATION_DURATION = {
  FAST: 200,
  NORMAL: 300,
  SLOW: 500,
} as const;

// Toast message durations
export const TOAST_DURATION = {
  SHORT: 2000,
  MEDIUM: 3000,
  LONG: 4000,
} as const;

// Pagination
export const ITEMS_PER_PAGE = 20;

// Search debounce delay (ms)
export const SEARCH_DEBOUNCE_DELAY = 300;

